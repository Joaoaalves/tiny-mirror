"""Queue subsystem — see :mod:`tiny_mirror.queue.bootstrap` for the
``start_consumers`` entry point and :mod:`tiny_mirror.queue.publisher`
for the publisher. The package ``__init__`` is intentionally bare to
avoid import cycles between services and consumers.
"""
